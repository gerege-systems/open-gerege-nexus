<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!--
        Ported from their Styles/Typography.xaml, with one substitution.
        Theirs derives from WinUI's TitleLarge / Title / Subtitle / Body /
        Caption styles, which WPF does not have — so each style states the size
        and weight those resolve to rather than inheriting them. The numbers are
        the ones their macOS Typography.swift already writes out longhand for
        exactly the same reason.
    -->

    <sys:Double x:Key="EidHeroTitleSize">28</sys:Double>
    <sys:Double x:Key="EidPageTitleSize">20</sys:Double>
    <sys:Double x:Key="EidSectionTitleSize">17</sys:Double>
    <sys:Double x:Key="EidBodySize">14</sys:Double>
    <sys:Double x:Key="EidLabelSize">12</sys:Double>

    <Style x:Key="HeroTitleTextBlock" TargetType="TextBlock">
        <Setter Property="FontSize" Value="28" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource EidTextPrimary}" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="PageTitleTextBlock" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource EidTextPrimary}" />
    </Style>

    <Style x:Key="SectionTitleTextBlock" TargetType="TextBlock">
        <Setter Property="FontSize" Value="17" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource EidTextPrimary}" />
    </Style>

    <!-- Their card heading: 24pt Bold, set directly in LoginView. -->
    <Style x:Key="CardTitleTextBlock" TargetType="TextBlock">
        <Setter Property="FontSize" Value="24" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{StaticResource EidTextPrimary}" />
    </Style>

    <Style x:Key="SubtleSubtitleTextBlock" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="{StaticResource EidMutedForeground}" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="LabelTextBlock" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{StaticResource EidMutedForeground}" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <!-- The identifier field: monospaced so digits line up as they are typed. -->
    <Style x:Key="IdentifierTextBox" TargetType="TextBox">
        <Setter Property="FontFamily" Value="Cascadia Mono, Consolas, Courier New" />
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
    </Style>
</ResourceDictionary>
